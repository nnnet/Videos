# AUTO-COMPACT HANDOFF — 2026-09-20T12:46:07+03:00 — sid 04195077-78db-4d32-bacf-8c3fbe65f27f

Сделано: проанализированы механизмы Claude Code 2.1.267 (docs/hooks, tools-reference): `run_in_background` → harness сам будит Клода по завершении процесса; Bash-`timeout` НЕ ловит зависание (команда уезжает в фон) → watchdog обязан жить в обёртке; `asyncRewake` exit 2 будит даже в idle; Stop-хук: `decision:"block"`+`reason`, вход содержит `background_tasks[]` (type=shell, command), лимит 8 блоков, флаг `stop_hook_active`. Пользователь утвердил схему и разрешил правки `.claude/hooks/`, `.claude/settings.json`, `CLAUDE.md`; отверг автозапуск тестов на Edit|Write («тесты только по решению Клода»). Память в `/home/node/.claude/...` недоступна (EACCES) — не сохранена.
Дальше: вызвать `Skill ak:plan` и создать OpenSpec-change `openspec/changes/<name>/` (proposal + spec delta `openspec/specs/<cap>/` + сценарий `bdd/features/*.feature` с тегом `@REQ-…` + tasks.md) со scope: (1) `scripts/test-bg.sh` — `timeout --kill-after=10 ${TEST_TIMEOUT:-600} pytest -c bdd/pytest.ini bdd`, статус PASS/FAIL(rc≠0)/HANG(124|137) в `.claude/state/test-result.json` {status,rc,log,acked:false}, echo `TESTS <st> rc=<n>` + tail лога; (2) `.claude/hooks/test-gate.sh` — Stop-хук: exit 0 если в `background_tasks` есть shell с `test-bg`, если файла нет, если PASS или acked=true; иначе `{"decision":"block","reason":"…FAIL→ak:debug/фикс/перезапуск; HANG→найти зависший шаг; acked:true+объяснить, если фикс невозможен"}`; (3) регистрация хука в `.claude/settings.json` (`Stop`, `${CLAUDE_PROJECT_DIR}/.claude/hooks/test-gate.sh`, `args:[]`); (4) правило в `CLAUDE.md`: тесты только через `scripts/test-bg.sh` с `run_in_background`, не ждать, реакция по PASS/FAIL/HANG. Затем аудит change отдельным агентом (`/ak:plan red-team` или ak:code-reviewer), правки по замечаниям, потом `/ak:cook`. Записи в `.claude/hooks`/`settings.json` требуют `dangerouslyDisableSandbox: true` — разрешение дано.
