# AUTO-COMPACT HANDOFF — 2026-09-20T14:28:12+03:00 — sid 04195077-78db-4d32-bacf-8c3fbe65f27f

Сделано: переписаны `scripts/test-bg.sh` (set -u, `write_verdict()`, RUNNING до старта, валидация `TEST_TIMEOUT`) и `scripts/test-gate.sh` (set -u, `cd $ROOT`, `tostring`, инверсия на терминальные статусы, дамп под `TEST_GATE_DEBUG`); краевые случаи прогнаны руками; задачи 1–2 change `background-tests-hardening` отмечены через `ak change check`. Ранее в этом же цикле: pyc убраны из индекса + `__pycache__/` в `.gitignore` (застейджено), задача 8 старого change прогнана живьём (блок → ack → молчание), реальный вердикт сейчас `FAIL acked=true`.
Дальше: `/ak:cook openspec/changes/background-tests-hardening/` — задачи 3 (feature: новые сценарии RUNNING/BUSY/rc=7/TEST_TIMEOUT=abc/started+числовой статус/дамп `dump=0`→`dump=1`; сценарий «молчание» печатает имя упавшего случая; названия сценариев должны совпадать с дельтой спеки; без backslash-кавычек и без `rm -rf`), 4 (CLAUDE.md: RUNNING, `TEST_GATE_DEBUG`), затем фоновый прогон `scripts/test-bg.sh` (ожидаемо красный до archive — только по spec-coverage? нет: pytest не зависит от archive, красный только pre-commit), `ak:code-reviewer` над diff, `openspec archive background-tests-hardening --yes`, `agent-ops spec-coverage .`, один коммит (включая pyc/.gitignore и change) и push — пользователь подтвердил «вместе с реализацией».
