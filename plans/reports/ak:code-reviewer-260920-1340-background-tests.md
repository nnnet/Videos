# Code review — OpenSpec change `background-tests` (2026-09-20 13:40)

Режим: advisory-only, правок не вносил. Прогон `pytest -c bdd/pytest.ini bdd` — 14 passed.
`bash -n` обоих скриптов — OK. `shellcheck` в системе отсутствует (проверка не выполнена).

## Находки

### blocker

1. **`.claude/settings.json` (ключ `env`) + `tasks.md:9`** — в HEAD файл был `{}`, в рабочем
   дереве появились `hooks` **и** `env.ANTHROPIC_AUTH_TOKEN` (литерал, 53 символа); задача 9
   планирует «коммит одним изменением» → токен уйдёт в git-историю.
   *Фикс:* перенести весь `env` в `.claude/settings.local.json` (файл уже есть, игнорируется
   глобальным `~/.config/git/ignore`), в `.claude/settings.json` оставить и закоммитить только `hooks`.

### major

2. **`scripts/test-bg.sh:22-25`** — если `jq` недоступен или падает, вердикт молча не пишется
   (проверено: остаётся `test-result.json.tmp`, обёртка выходит с rc тестов), и гейт продолжает
   читать прошлый вердикт — ровно тот пропуск FAIL, ради которого change делался.
   *Фикс:* `command -v jq >/dev/null || { echo "TESTS ERROR: нет jq"; exit 4; }` в начале и
   `rm -f` временного файла при неуспехе `jq`.
3. **`scripts/test-bg.sh:20`** — на старте прогона старый вердикт не инвалидируется; от ложной
   блокировки защищает только разбор `background_tasks`, форма shell-записи живьём не подтверждена
   (в единственном дампе `.claude/state/last-stop.json` есть лишь `type:"subagent"`, задача 8 открыта).
   *Фикс:* писать перед запуском вердикт `status:"RUNNING"` и трактовать его в гейте как молчание.

### minor

4. **`scripts/test-gate.sh:19-21`** — нестроковый `status` в любом элементе `background_tasks`
   рвёт весь jq-фильтр, и running-запись рядом уже не спасает (проверено: блок вместо молчания).
   *Фикс:* `((.status // "running") | tostring | ascii_downcase | test(...))`.
5. **`scripts/test-gate.sh:21`** — белый список «работающих» статусов: неизвестное значение
   (`started`) считается завершением и даёт блок посреди живого прогона (проверено).
   *Фикс:* инвертировать условие — молчать на всём, кроме терминальных статусов.
6. **`scripts/test-gate.sh:11`** — недокументированный дамп события Stop в `<state>/last-stop.json`
   на каждый ход (остаток задачи 1), в спеке и design.md его нет.
   *Фикс:* убрать или включать по `TEST_GATE_DEBUG=1` и описать в спеке.
7. **`scripts/test-bg.sh:6,17` vs `scripts/test-gate.sh:7`** — относительный `TEST_STATE_DIR`
   обёртка резолвит от корня репозитория (проверено: создала `<repo>/relstate`), гейт — от cwd хука.
   *Фикс:* нормализовать путь от `ROOT` и в гейте.
8. **`scripts/test-bg.sh:15,20`** — нечисловой `TEST_TIMEOUT` даёт rc=125 и вердикт `FAIL`, хотя
   упала обёртка, а не тесты (проверено).
   *Фикс:* валидировать `[[ $LIMIT =~ ^[0-9]+$ ]]` до запуска.
9. **`design.md:73-97`, `tasks.md:7`** — эскиз расходится с кодом (отказ/BUSY в stderr против stdout,
   нет нормализации `ST`, нет дампа `last-stop.json`; обещан `TEST_STATE_DIR=$(mktemp -d)`, в feature —
   `.claude/state/bdd/*`).
   *Фикс:* синхронизировать design/tasks до `openspec archive`.
10. **`spec.md` REQ-001** — «второй одновременный запуск» и «rc обёртки = rc прогона» заявлены в
    прозе, но WHEN/THEN-сценария нет (проверялось только руками).
    *Фикс:* добавить сценарий на `flock` (`TESTS BUSY`, rc=3).
11. **`bdd/features/background-tests.feature:62`** — пять условий в одном однострочнике со счётчиком
    `silent=5/5`: при падении не видно, какое именно условие сломалось.
    *Фикс:* «Структура сценария» + «Примеры».
12. **`scripts/*.sh`** — часть скриптов репо (`copy_to_Folder3.sh` и др.) идёт с `set -euo pipefail`,
    новые — без; `cd ""` при неудачном вычислении `ROOT` проходит молча.
    *Фикс:* добавить хотя бы `set -u` (для `test-bg.sh` `set -e` неуместен — ломает захват rc).
13. **`bdd/steps/__pycache__/repo_steps.cpython-312.pyc`** — отслеживаемый `.pyc` изменён и попадёт
    в коммит вместе с change.
    *Фикс:* `git rm --cached` каталогов `__pycache__` и запись в `.git/info/exclude`.

## Проверено и претензий нет

- Покрытие (a): все 11 WHEN/THEN-сценариев спеки один в один присутствуют в feature-файле
  (REQ-001 — 4, REQ-002 — 5, REQ-003 — 2), теги проставлены.
- Краевые случаи гейта: пустой stdin, не-JSON, битый вердикт, отсутствующий `jq`, отсутствующий
  каталог state → rc=0, пустой вывод; `exit 2` не встречается нигде.
- Watchdog: `TEST_TIMEOUT` → rc=124 → HANG; `trap "" TERM` + `--kill-after` → rc=137 → HANG; rc обёртки
  равен rc прогона.
- Атомарность: `tmp` + `mv` в том же каталоге; `flock -n` на втором запуске даёт `TESTS BUSY`, rc=3.
- Маркер `AUTO-COMPACT-READY` реально используется глобальным `~/.claude/hooks/stop-detect-compact.sh`
  — проверка в гейте не мёртвая.
- Регрессии (b): в HEAD у `.claude/settings.json` ключа `hooks` не было — чужие проектные хуки не
  затронуты; глобальные хуки и скрипты пайплайна не менялись; существующие bdd-сценарии зелёные.
- Контракты (c): имена `TEST_CMD`/`TEST_TIMEOUT`/`TEST_STATE_DIR`/`TEST_RESULT_TTL_MIN`, поля вердикта
  `status/rc/log/ts/cwd/acked` и путь `.claude/state` совпадают в spec, design, CLAUDE.md и скриптах.
- `agent-ops spec-coverage .` красный (3 расхождения) — ожидаемо до `openspec archive`, это задача 9.

## Вердикт

**needs-changes** — из-за находки 1 (секрет в файле, который планируется закоммитить) и двух major.
Остальное можно закрывать после.

## Нерешённые вопросы

1. Реальная форма записи shell-задачи в `background_tasks[]` (есть ли `.command`, точное значение
   `type`) — не подтверждена живьём; задача 8 открыта, от неё зависит вся защита «тесты ещё бегут».
2. Куда переносить `env` из `.claude/settings.json` — решение пользователя (предложение:
   `.claude/settings.local.json`).
3. Игнорировать ли `stop_hook_active` (принято «да, блокировать повторно до потолка 8») — подтвердить.
